var inputs = document.querySelectorAll('campo__input');

inputs.forEach(input => {
    input.addEventListener('onchange', function (evt) {
        valorDigitado = input.value;
        valorMaiusculo
    })
})
